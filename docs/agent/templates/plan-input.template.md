# Plan Input

story_id: <story-id>
captured_at: <iso-timestamp>
source: Plan agent | user | jira | rag | other

## Input type
One of:
- jira story (id + acceptance criteria)
- feature description (free-form text)
- direct user request
- RAG-derived task (link + context)

## Raw handoff
- Paste the input exactly as received. Do not rewrite intent here — brainstorm is where refinement happens.
